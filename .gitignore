# Secrets / local keys (root SSH key for the throwaway test VM)
vm-key

# Large VM test artifacts (generated by scripts/build-vm-image.sh)
lowgui-test.img
vmlinuz
initrd.img

# Local dev tooling / runtime state
mcp.json
.kilo/
imgui.ini

# Build and test output
opencv/
build/
test-results/